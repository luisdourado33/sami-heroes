import React from 'react';
import './styles.scss';

import CardScope from 'src/molecules/CardScope';

const HeroCard = () => {
  return (
    <div>
      <CardScope />
    </div>
  );
};

export default HeroCard;
